import React from 'react';
import { View, Text } from 'react-native';

const ProfileScreen = () => {
  return (
    <View>
      <Text>Profile Screen</Text>
      {/* Add UI to manage account settings, view input history */}
    </View>
  );
};

export default ProfileScreen;
